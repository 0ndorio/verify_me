# Setup local repository

- git clone --recursive https://github.com/0ndorio/verify_me.git

# Setup client side development

- cd [PROJECT_DIR]/client
- npm install

> Run karma with: ./node_modules/.bin/karma start karma.conf.js
> Run watchify with ./node_modules/,bin/watchify src/main.js -o dist/verify_me.js

# Setup test server

- cd [PROJECT_DIR]/
- vagrant up

> Attention: The bundled client side js is currentyl not included in the repository. Please use browserify/watchify to create it as ./client/dist/verify_me.js

- vagrant ssh
- sudo cp /vagrant/client/dist/verify_me.js ~/[PROJECT_NAME]/client/dist/verify_me.js
- /bin/bash ~/[PROJECT_NAME]/utility/run_server.sh 
